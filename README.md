# fast-disambig

Attempting to write a fast, lean morphology database, with python bindings to work with most projects. 
Not many deps

Thinking of starting with camel's builtin-db and support their format entirely

then moving to other formats (sinatools' is interesting)
